# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# --- Buildozer / Android (сборка идёт в CI, локальные артефакты не коммитим) ---
.buildozer/
bin/
*.apk

# --- IDE ---
.idea/
.vscode/

# ВНИМАНИЕ: lessons.db НАМЕРЕННО не игнорируется — тестовая БД должна попадать в git
# (по требованию: база с тестовыми данными пушится на GitHub).
# !lessons.db